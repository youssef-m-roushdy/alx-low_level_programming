/**
 * div - Computes the result of the division of
 * the first parameter by the second.
 * @a: The dividend.
 * @b: The divisor.
 *
 * Return: The result of the division (a / b).
 */
int div(int a, int b)
{
	return (a / b);
}
