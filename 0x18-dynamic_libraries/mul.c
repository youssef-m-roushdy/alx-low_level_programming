/**
 * mul - Multiplies the two parameters.
 * @a: The first operand.
 * @b: The second operand.
 *
 * Return: The result of the multiplication (a * b).
 */
int mul(int a, int b)
{
	return (a * b);
}
